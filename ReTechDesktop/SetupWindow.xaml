<Window x:Class="ReTechDesktop.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="İlk Kurulum - KobiX" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Textbox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#f97316"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ea580c"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="#171717" CornerRadius="12" BorderBrush="#262626" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="3" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <!-- Title Bar -->
            <Grid Height="40" VerticalAlignment="Top">
                <TextBlock Text="Firma Kaydı / İlk Kurulum" Foreground="#a3a3a3" VerticalAlignment="Center" Margin="15,0" FontFamily="Segoe UI" FontSize="12"/>
                <Button Content="✕" Width="40" Height="40" HorizontalAlignment="Right" Background="Transparent" Foreground="#a3a3a3" BorderThickness="0" Click="CloseButton_Click"/>
            </Grid>

            <!-- Form Content -->
            <StackPanel Margin="20,50,20,20">
                <TextBlock Text="Firma Adı:" Foreground="#a3a3a3" FontFamily="Segoe UI" FontSize="12"/>
                <TextBox x:Name="TxtCompanyName" Style="{StaticResource ModernTextBox}" />

                <TextBlock Text="Vergi Numarası:" Foreground="#a3a3a3" FontFamily="Segoe UI" FontSize="12"/>
                <TextBox x:Name="TxtTaxNumber" Style="{StaticResource ModernTextBox}" />

                <TextBlock Text="Yönetici Adı Soyadı:" Foreground="#a3a3a3" FontFamily="Segoe UI" FontSize="12"/>
                <TextBox x:Name="TxtAdminName" Style="{StaticResource ModernTextBox}" />

                <TextBlock Text="E-Posta (Giriş İçin):" Foreground="#a3a3a3" FontFamily="Segoe UI" FontSize="12"/>
                <TextBox x:Name="TxtEmail" Style="{StaticResource ModernTextBox}" />

                <TextBlock Text="Şifre:" Foreground="#a3a3a3" FontFamily="Segoe UI" FontSize="12"/>
                <PasswordBox x:Name="TxtPassword" Style="{StaticResource ModernPasswordBox}" />

                <Button Content="Sistemi Kur ve Kaydet" Style="{StaticResource ModernButton}" Margin="0,10,0,0" Click="BtnSave_Click"/>
                
                <TextBlock x:Name="TxtStatus" Text="" Foreground="#f97316" FontFamily="Segoe UI" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
